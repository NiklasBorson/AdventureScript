# String and FormatString
$var = "String"
$"Format {$var}" "Literal String" 

# Integers and symbols
0xf800
(10 - 2) * (7 + 1)/3
, || && ! ; : = < > == <= >= != => ->

# Function example
function($arg1:item, $arg2:item) : item
{
   $varname = $arg1.Prop; # comment
}
